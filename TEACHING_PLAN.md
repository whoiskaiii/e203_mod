# 蜂鸟 E203 教学计划（e203_mod）

## 教学模式

- 老师（Codex）：讲解 → 演示 → 布置练习 → 验收出题 → 答疑
- 学生（你）：听课 → 动手跑仿真/看波形/改代码 → 回答验收题 → 随时提问
- 场地：虚拟机 ~/hkq/e203_mod；你可在自己终端操作，也可让我代跑
- 节奏：每课约 1-2 小时，自定进度；任何一课没吃透就不进入下一课
- 原则：不背代码，讲原理；先看波形后看代码；每课必练、练完必验

## 课程总览

| 课 | 主题 | 过关标准 |
|---|---|---|
| 0 | 环境与仿真流程 | 独立跑通测试、打开波形、看懂 log |
| 1 | RISC-V 基础 + E203 全局架构 | 画出模块层级图，讲清三条数据通路 |
| 2 | IFU 取指 | 讲清两级流水前半段；改一次分支预测 |
| 3 | EXU 执行 | 讲清译码/派遣/乱序完成；加一个 ALU 运算 |
| 4 | LSU / 存储 / ICB | 画出一笔 load 的完整旅程 |
| 5 | 中断 / 调试 / 外设 | 画出中断响应全流程 |
| 6 | FPGA 上板（进阶） | 板卡跑通程序并记录跑分 |

每课流程（固定）：
1. 摸底：我先问 2 个问题，看你的起点
2. 讲解：用大白话 + 代码 + 波形讲清概念
3. 演示：我在 VM 上跑给你看
4. 练习：你动手（改代码/跑仿真/看波形）
5. 验收：我出 2-3 道题，你回答；答对过关，答错重讲

## 第 0 课：环境与仿真流程

目标：成为"仿真操作员"——以后每一课都靠这套流程验证。

讲解要点：
- vsim 四个脚本的分工（Makefile 编排 / run.makefile 干活 / README+CMDE 说明书）
- install→compile→run_test→wave 每一步在干什么
- TESTNAME 与 TESTCASE 的区别；DUMPWAVE 的作用
- log 里看什么：Instruction Count / cycle / TEST_PASS

练习：
- [ ] 自己敲一遍 make install / make run_test / make wave
- [ ] 在 Verdi 里找到 ifu_pc 信号并看到它变化
- [ ] 跑 rv32mi-p-csr 和 rv32ui-p-addi，对比 log

验收题：
1. 为什么第一次必须 make install，之后可以跳过？
2. simv 是在哪个目录生成的？wave 打开的波形文件叫什么？
3. 如果改了 RTL 但仿真没变化，第一步应该做什么？

## 第 1 课：RISC-V 基础 + E203 全局架构

目标：脑子里有 E203 的完整地图。

讲解要点：
- RV32I：寄存器、指令格式、CSR、异常/中断概念（对照 doc/riscv-spec-v2.2.pdf）
- E203 的 2 级流水 vs 经典 5 级流水，为什么这样设计
- 从 soc_top 到 core 的层级；ITCM/DTCM 地址空间
- config.v / e203_defines.v 读法

练习：
- [ ] 画出模块层级图（我会带你画第一版）
- [ ] 在 Verdi 里对照层级图找实例
- [ ] 用一句话给每个模块写职责

验收题：
1. 一条指令在 E203 里被分成哪两个流水阶段？
2. 0x8000_0000 和 0x9000_0000 是什么？
3. mret 指令的用途？

## 第 2 课：IFU 取指

目标：讲清"指令是怎么被拿进来的"。

讲解要点：
- ifetch/idecode 两级；PC 逻辑；指令对齐
- mini 译码器：为什么要在取指阶段先"偷看"指令
- litebpu：最简单的分支预测长什么样
- ift2icb：取指请求如何变成 ICB 事务

练习：
- [ ] Verdi 跟踪 beq/jal 的 PC 变化
- [ ] 修改 litebpu 参数，对比周期数变化

验收题：
1. 分支预测错了会怎样？E203 怎么处理？
2. mini 译码器在流水线里承担什么角色？

## 第 3 课：EXU 执行

目标：讲清"指令是怎么被算出来的"。

讲解要点：
- 译码→派遣→执行→提交 的路径
- alu 各子模块（dpath/rglr/bjp/csrctrl/muldiv）分工
- OITF：什么是乱序完成、有序提交，为什么需要
- 写回与长流水线写回（longpwbck）

练习：
- [ ] 画出 addi 和 beq 的完整时序图
- [ ] 在 alu_dpath 加一个自定义运算并跑通

验收题：
1. E203 最多有几条指令"在飞"？OITF 怎么跟踪？
2. muldiv 为什么单独有 longpwbck？

## 第 4 课：LSU / 存储 / ICB

目标：讲清"数据是怎么被读写的"。

讲解要点：
- LSU 流水：load/store 类型、字节/半字处理
- ITCM/DTCM 控制器的职责；SRAM 接口
- ICB 协议：命令通道/返回通道、仲裁、qos
- BIU：内核与外设世界的桥梁

练习：
- [ ] 跑 rv32mi-* / rv32ua-* 回归
- [ ] 改 DTCM 容量并验证
- [ ] Verdi 抓一笔 ICB 读事务时序

验收题：
1. 一次 load 从 LSU 发出到数据回来，经过哪些模块？
2. ICB 和 AXI 最大的区别是什么？

## 第 5 课：中断 / 调试 / 外设

目标：讲清"突发事件怎么被处理"。

讲解要点：
- CLINT（mtime/msip）与 PLIC（优先级）
- 中断响应：取指到 mtvec → 保存 mepc/mstatus → mret
- JTAG→DTM→DM 调试链路（只讲流程）
- 外设怎么通过总线挂进来（uart/gpio 举例）

练习：
- [ ] 跑 tb 中断用例，Verdi 跟踪完整流程
- [ ] 用 sirv-e-sdk 编译 demo_gpio 并仿真验证

验收题：
1. 中断和异常的区别？
2. mret 时 CPU 恢复了哪些状态？

## 第 6 课：FPGA 上板（进阶）

目标：把学到的东西跑在真硬件上。

讲解要点：
- my_325t 板卡工程结构；Vivado 流程
- 仿真与板卡的行为差异（时钟/复位/异步）
- dhrystone/coremark 怎么跑、怎么读跑分

练习：
- [ ] 复现 hello world 上板
- [ ] 板卡跑 dhrystone/coremark 记录跑分
- [ ] 进阶：EAI 自定义指令上板 或 DC 综合

验收题：
1. 仿真 PASS 的程序上板跑不起来，可能原因有哪些？
2. 你的板卡跑分和仿真周期数之间是什么关系？

## 上课纪律

1. 每课开始先回答摸底题，不猜、不会就说"不会"
2. 练习必须亲手做，做不出来先自己试 10 分钟再求助
3. 我讲的任何一段，随时可以要求"再讲一遍"或"换个角度"
4. 每课验收通过后，我会在 TEACHING_PLAN.md 里勾掉该课